name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Caching
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    - name: Format
      run: cargo fmt --check
    - name: Clippy
      run: cargo clippy -- -D warnings
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Caching
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    - name: Install Wasm32-wasi target
      run: rustup target add wasm32-wasi
    - name: Build Rust examples
      working-directory: ./examples
      run: make all
    - name: Check existing wws release (Unix)
      id: check_file_release
      if: matrix.os != "windows-latest"
      uses: andstor/file-existence-action@v2
      with:
        files: "target/release/wws"
    - name: Check existing wws release (Windows)
      if: matrix.os == "windows-latest"
      id: check_file_release
      uses: andstor/file-existence-action@v2
      with:
        files: "target/release/wws.exe"
    - name: Build wws on release mode
      if: steps.check_file_release.outputs.files_exists == 'false'
      run: cargo build --verbose --release
    - name: Test
      run: cargo test --workspace --exclude wasm-workers-quick-js-engine -- --show-output
